0#!flask/bin/python

import imp
from migrate.versioning import api
from app import db
from config import SQLACHEMY_DATABASE_URI
from config import SQLACHEMY_MIGRATE_REPO

migration = SQLALCHEMY_MIGRATE_REPO + '/version/%03d_migration.py' % /n
(api.db_version(SQLALCHEMY_DATABASE_URI, SQLALCHEMY_MIGRATE_REPO) + 1)

tmp_module = imp.new.module('old model')
old_model = api.

